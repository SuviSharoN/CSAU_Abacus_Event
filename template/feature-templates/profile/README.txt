PROFILE PAGE - BUILD CHALLENGE

What You Need to Code:
----------------------

1. HTML (index.html):
   - Build the full profile form inside `.profile-form-wrapper`
   - Create these required ids:
     - profileForm
     - teamIdDisplay (readonly input)
     - teamName
     - leadName
     - leadEmail
     - teamSize
     - bio
     - phone
     - organization
     - saveProfileBtn
     - successMessage

2. CSS (style.css):
   - Base form styling is already provided
   - Improve/tweak it to better match the ABACUS dark + gold theme in the reference site
   - Focus on polishing spacing, colors, and states

3. JavaScript (script.js):
   - Only 1 coding task in JS:
     - Complete `validateProfileForm(profileData)`
     - Add basic form checks:
       - required fields are filled
       - valid email format
       - team size range check (1 to 5)
       - optional phone format check
   - All other JS wiring is already provided

Expectation:
-----------
- Participants should build the profile form UI from scratch in this task page.
- The form behavior should match the reference website.
- On submit, invalid form should show an error alert.
- On valid form, data should be logged and success message should show briefly.

