🏆 LEADERBOARD PAGE - BUILD CHALLENGE

What You Need to Code:
----------------------

1. HTML (index.html):
   - Already has table structure
   - Just need to fill tbody with rows

2. CSS (style.css):
   - Already has most styling
   - Just style the table rows and cells

3. JavaScript (script.js):
   - Function 1: renderLeaderboard() - Create table rows from teams array
   - Function 2: sortTeams() - Sort by points or events and re-render

Expectation:
-----------
- Display 10 teams in a table (Rank, Name, Points, Events)
- Click "Sort by Points" → Re-sort table
- Click "Sort by Events" → Re-sort table
- Top 3 rows have special background colors (gold, silver, bronze)
- Hover effect on rows

That's it!

Tips:
-----
- Use <table> HTML element for structured data
- Re-rank teams after sorting
- Display medal emoji (🥇 🥈 🥉) for top 3
- Highlight your team with a different background color
- Show "You are X points ahead/behind the top team"
- Make table scrollable on mobile
- Use 2-3 decimal places for points display
